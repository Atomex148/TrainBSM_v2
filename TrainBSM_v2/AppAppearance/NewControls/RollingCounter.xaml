<UserControl x:Class="TrainBSM_v2.AppAppearance.NewControls.RollingCounter"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TrainBSM_v2.AppAppearance.NewControls">
    <Grid x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" CornerRadius="8" BorderThickness="2" BorderBrush="Black" Padding="10,10"
        Background="White">
            <Border CornerRadius="5" BorderThickness="1.5" BorderBrush="#FFD0D0D0" Padding="8,7"
            Background="White">
                <StackPanel x:Name="DigitPanel" Orientation="Horizontal" HorizontalAlignment="Center"/>
            </Border>
        </Border>

        <TextBlock x:Name="NameText" Text="Counter" Grid.Row="1"
           FontSize="11" FontWeight="Bold" Foreground="Black"
           HorizontalAlignment="Center" VerticalAlignment="Top" 
           TextWrapping="Wrap" TextAlignment="Center" 
           TextTrimming="CharacterEllipsis"
           Margin="2,5,2,2" 
           MaxWidth="200"/>
    </Grid>
</UserControl>